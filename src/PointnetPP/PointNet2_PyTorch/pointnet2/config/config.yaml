defaults:
    - task: semseg  # Changed from cls to semseg for semantic segmentation
    - model: msg    # Multi-scale grouping (better for outdoor LiDAR)
    - task_model: ${defaults.0.task}-${defaults.1.model}

hydra:
  run:
    dir: outputs

gpus:
    - 0

optimizer: ???

task_model: ???

model: ???

distrib_backend: dp
